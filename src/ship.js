export default class ship {
  constructor (shipType) {
    this.name = shipTyper.name,
    this.icon = shipType.icon,
    this.size = shipType.size,
    this.sunk = false
  }

  placeShip(coordinates, direction){
    //coordinates + this.length by the direction 
    // then check if valid placement
    //if valid replace spaces on board with ship icons
  }

}